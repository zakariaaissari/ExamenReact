# Screenshots

Images referenced by the root `README.md`. `releve.png` is a real render of the
generated transcript PDF. The rest are placeholders — run the frontend
(`cd frontend && npm run dev`, open http://localhost:5173), capture each screen,
and drop the PNG here with the exact filename below.

| Filename         | Screen                                             | Status      |
|------------------|----------------------------------------------------|-------------|
| `login.png`      | Login page (admin / admin)                         | _to add_    |
| `home.png`       | Home dashboard with programs/students/grades counts| _to add_    |
| `programs.png`   | Programs list + add/edit modal                     | _to add_    |
| `students.png`   | Students list with program + Relevé action         | _to add_    |
| `notes.png`      | Grades (notes) list                                | _to add_    |
| `releve.png`     | Relevé de notes (transcript export)                | ✅ included |
